(function(window){
	var iLearner = window.iLearner || {},
		Conf = iLearner.Conf || {};

	window.iLearner = iLearner;
	iLearner.Conf = Conf;

	/*
	 * Uncomment as neccessary
	 */

	// Conf.API_ROOT = "";

	// Conf.PHOTO_URL = "/photos";

}(window));
